<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title></title>
</head>
<body>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
                  url('https://t3.ftcdn.net/jpg/13/36/43/62/360_F_1336436200_rHeY7imdqVDd49A3tCM4kGUcQZuNdAyD.jpg');
      background-size: cover;
      background-position: center;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      color: white;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 2rem;
      color: #ffffff;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    .container {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      padding: 2rem;
      border-radius: 15px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      text-align: center;
      max-width: fit-content;
      width: 100%;
    }

    .button-container {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .action-button {
      display: inline-block;
      padding: 1rem 2rem;
      background: #4CAF50;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      transition: transform 0.2s, background 0.2s;
    }

    .action-button:hover {
      transform: translateY(-2px);
      background: #45a049;
    }

    .view-button {
      background: #2196F3;
    }

    .view-button:hover {
      background: #1976D2;
    }
  </style>

  <div class="container">
    <h1>Hi Welcome </br> Book  Your Tickets Here</h1>
    <div class="button-container">
      <div><%= link_to "Book Tickets", userticket_path, class: "action-button" %></div>
      <div><%= link_to "View Ticket", ticketdetails_path, class: "action-button view-button" %></div>
    </div>
  </div>
</body>
</html>